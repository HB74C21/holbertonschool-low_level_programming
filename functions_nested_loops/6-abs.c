#include "main.h"
/**
 * _abs - Function that computes the absolute value of an integer
 *
 * @n: This is a integer
 *
 * Return: The absolute value
*/
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-n);
}
